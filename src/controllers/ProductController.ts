import { Request,Response } from "express";

export class ProductController {
    static createProduct = (req:Request, res:Response) => {
        try{

        }catch(error){
            console.log(error);
        }
    }

    static getAllProducts = (res:Response) => {
        try{
            
        }catch(error){
            console.log(error);
        }
    }
}